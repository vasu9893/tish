[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  VITE_API_URL = "https://tish-production.up.railway.app"

# Optional: route everything to index.html (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
